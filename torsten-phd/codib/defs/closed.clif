(cl-module codib/defs/closed

(cl-comment "Defining when an entity is closed (closed manifold)")

(cl-comment "CL-D: closed")

(forall (x)
	(iff
		(Closed x)
		(forall (y)
			(not (BCont y x))
		)
	)
)

)