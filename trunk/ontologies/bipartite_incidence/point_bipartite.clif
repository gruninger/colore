
(cl-text http://colore.oor.net/biparite_incidence/point_bipartite.clif

(cl-imports http://colore.oor.net/biparite_incidence/weak_bipartite.clif)

(forall (p)
	(if	(point p)
		(exists (l)
			(and	(line l)
				(in p l)))))

)
