Paradox, version 4.0, 2010-06-29.
+++ PROBLEM: /stl/torsten/git/macleod/qs/multidim_space_codi/definitions/conversions/point.all.tptp
Reading '/stl/torsten/git/macleod/qs/multidim_space_codi/definitions/conversions/point.all.tptp' ... OK
+++ SOLVING: /stl/torsten/git/macleod/qs/multidim_space_codi/definitions/conversions/point.all.tptp
domain size 1
Verified 25 original and 0 eliminated clauses.
+++ BEGIN MODEL
SZS output start FiniteModel for /stl/torsten/git/macleod/qs/multidim_space_codi/definitions/conversions/point.all.tptp
% domain size is 1
fof(domain, fi_domain,
  (![X] : (X = "1"))
).

fof(clifsym1, fi_predicates,
  ( (clifsym1("1","1") <=> $true)
  )
).

fof(clifsym2, fi_predicates,
  ( (clifsym2("1","1") <=> $false)
  )
).

fof(cont, fi_predicates,
  ( (cont("1","1") <=> $true)
  )
).

fof(eqdim, fi_predicates,
  ( (eqdim("1","1") <=> $true)
  )
).

fof(leq, fi_predicates,
  ( (leq("1","1") <=> $true)
  )
).

fof(less, fi_predicates,
  ( (less("1","1") <=> $false)
  )
).

fof(max, fi_predicates,
  ( (max("1") <=> $true)
  )
).

fof(maxdim, fi_predicates,
  ( (maxdim("1") <=> $true)
  )
).

fof(min, fi_predicates,
  ( (min("1") <=> $true)
  )
).

fof(mindim, fi_predicates,
  ( (mindim("1") <=> $true)
  )
).

fof(p, fi_predicates,
  ( (p("1","1") <=> $true)
  )
).

fof(point, fi_predicates,
  ( (point("1") <=> $true)
  )
).

fof(pp, fi_predicates,
  ( (pp("1","1") <=> $false)
  )
).

fof(s, fi_predicates,
  ( (s("1") <=> $true)
  )
).

fof(sK0_sos10_X11, fi_functors,
  ( (sK0_sos10_X11("1") = "1")
  )
).

fof(sK18_sos80_X81, fi_functors,
  ( (sK18_sos80_X81("1") = "1")
  )
).

fof(sK21_sos90_X91, fi_functors,
  ( (sK21_sos90_X91("1") = "1")
  )
).

fof(sK34_sos150_X151, fi_functors,
  ( (sK34_sos150_X151 = "1")
  )
).

fof(sK3_sos20_X21, fi_functors,
  ( (sK3_sos20_X21("1") = "1")
  )
).

fof(zex, fi_predicates,
  ( (zex("1") <=> $false)
  )
).
SZS output end FiniteModel for /stl/torsten/git/macleod/qs/multidim_space_codi/definitions/conversions/point.all.tptp
+++ END MODEL
+++ RESULT: Satisfiable
SZS status Satisfiable for /stl/torsten/git/macleod/qs/multidim_space_codi/definitions/conversions/point.all.tptp
============================= paradox ================================
execution finished: Tue Oct 08 18:29:46 2013
total CPU time used: 0
The command was "paradox --time 3000 --verbose 2 --model --tstp /stl/torsten/git/macleod/qs/multidim_space_codi/definitions/conversions/point.all.tptp"
Input read from 
============================ end of footer ===========================
