
(cl-text cem_C

(cl-imports cem_mereology)

(cl-imports mereology_def)

(cl-comment "Everything which is not the greatest element has a complement.")
(forall (x w)
	(if	(not (part w x))
		(exists (z)
			(forall (y)
				(iff	(part y z)
					(not (overlaps y x)))))))

)
