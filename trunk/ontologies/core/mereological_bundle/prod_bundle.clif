
(cl-text prod_bundle

(cl-imports m_bundle)

(cl-imports mereological_bundle_def)

(forall (x y)
	(if	(overlaps x y)
		(exists (z)
			(forall (v)
				(iff	(tppart v z)
					(and	(tppart v x)
						(tppart v y)))))))

)
