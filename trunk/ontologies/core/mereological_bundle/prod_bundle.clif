
(cl-text prod_bundle

(cl-imports m_bundle)

(cl-imports mereological_bundle_def)

(forall (x y p)
	(if	(overlaps x y p)
		(exists (z)
			(and	(line z)
				(forall (v)
					(if	(line v)
						(iff	(tppart v z p)
							(and	(tppart v x p)
								(tppart v y p)))))))))

)
