
(cl-text mg_def

(cl-imports wmg)

(cl-imports ../mereology/mereology_def)

(forall (l1 l2)
	(iff	(atomic_segment l1 l2)
		(and	(exists (p1)
				(and	(point p1)
					(in p1 l1)))
			(forall (p2)
				(if	(and	(point p2)
						(atomic_part p2)
						(in p2 l1))
					(in p2 l2))))))
			

)
