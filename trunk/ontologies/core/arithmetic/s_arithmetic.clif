
(cl-text successor)

(forall (x)
	(not (= (S zero))))

(forall (x y)
	(if	(= (S x) (S y))
		(= x y)))

(forall (y)
	(if	(not (= y zero))
		(exists (x)
			(= y (S x)))))

(forall (x)
	(not (= x (S ...))))

)
