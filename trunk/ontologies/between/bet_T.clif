
(cl-text bet_T

(cl-imports betweenness)

(forall (x y z w)
        (if     (and    (between x y z)
                        (between y z w))
                (between x y w)))

(forall (x y z w)
        (if     (and    (between x y w)
                        (between y z w))
                (between x y z)))

)

