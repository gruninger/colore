
(cl-text http://colore.oor.net/between/bet

(cl-imports http://colore.oor.net/between/betweenness)

(forall (x y z w)
        (if     (and    (between x y z)
			(not (= y z))
                        (between y z w))
                (between x y w)))

(forall (x y z w)
        (if     (and    (between x y w)
                        (between y z w))
                (between x y z)))

)

