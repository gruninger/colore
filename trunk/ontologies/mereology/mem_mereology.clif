
(cl-text mem_mereology

(cl-comment "Minimal Extensional Mereology Simons 1987")

(cl-imports m_mereology)

(cl-comment "Product")
(forall (x y)
        (if     (overlaps x y)
                (exists (z)
                        (forall (v)
                                (iff    (ppart v z)
                                        (and    (ppart v x)
                                                (ppart v y)))))))

)
