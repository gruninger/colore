(cl-text http://colore.oor.net/arithmetic/succ_less.clif

(cl-imports linear_ordering)

(forall (y)
	(if	(not (= y zero))
		(exists (x)
			(= y (S x)))))

(forall (x y)
	(iff	(lt x (S y))
		(leq x y)))

(forall (x)
	(not (lt x zero)))

)