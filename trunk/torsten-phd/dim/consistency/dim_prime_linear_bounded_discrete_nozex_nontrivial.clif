(cl-module  dim/consistency/dim_prime_linear_bounded_discrete_nozex_nontrivial

(cl-imports  dim/dim_prime_linear_bounded_discrete)
(cl-imports  zex/nozex)


(exists (x)
	(and
		(not (MaxDim x))
		(not (MinDim x))
	)
)

)
