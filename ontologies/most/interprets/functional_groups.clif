(cl-text to_prove_functional_groups

(cl-imports http://colore.oor.net/most/definitions/most_shape.clif)

(cl-comment 'An axiom to prove')
(forall (x)
	(iff (functional_group x)
		(or (ring x)(chain x))))

)