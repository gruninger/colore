
(cl-text http://colore.oor.net/biparite_incidence/partial_bipartite.clif

(cl-imports http://colore.oor.net/biparite_incidence/weak_bipartite.clif)

(cl-comment "Every line has a point incident with it")

(forall (l)
	(if	(line l)
		(exists (x)
			(and	(point x)
				(in x l)))))

)
