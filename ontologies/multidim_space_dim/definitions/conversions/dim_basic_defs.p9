% Automatically generated from CL/KIF
formulas(sos).
% 'http://colore.oor.net/multidim_space_dim/definitions/dim_basic_defs.clif'
% imports("http://colore.oor.net/multidim_space_dim/definitions/eq_dim.clif").
 all x all y ((>(x, y) <-> <(y, x))).
 all x all y ((>=(x, y) <-> <=(y, x))).
 all x all y ((<(x, y) <-> (<=(x, y) & -(EqDim(x, y))))).

end_of_list.
