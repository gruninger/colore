
(cl-text http://colore.oor.net/ringoids/division_ring.clif

(cl-imports http://colore.oor.net/ringoids/ring.clif)

(not (= zero one))

(forall (x)
	(if	(not (= x zero))
		(exists (y)
			(and	(= one (prod x y))
				(= one (prod y x))))))

)
