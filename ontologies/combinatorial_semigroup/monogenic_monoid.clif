
(cl-text http://colore.oor.net/combinatorial_semigroup/monogenic_monoid.clif

(cl-imports http://colore.oor.net/combinatorial_semigroup/concat_quine_monoid.clif)

(forall (x y)
	(if	(and	(generator x)
			(generator y))
		(= x y)))
		

)
