
(cl-text oa

(cl-imports ../ordering/discrete_forest)

(cl-imports ../cayley_graph/cayley_graph_root)

(forall (x y z)
	(if	(generator y)
		(iff	(lt x (append y z))
			(leq x z))))

(forall (x y)
	(if	(lt x y)
		(exists (z w)
			(= y (append z w)))))

)
