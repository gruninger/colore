
(cl-text sum_bundle

(cl-imports m_bundle)

(cl-imports mereologcal_bundle_def)

(forall (x y l)
	(if	(underlaps x y l)
		(exists (z)
			(forall (v)
				(iff	(overlaps v z l)
					(or	(overlaps v x l)
						(overlaps v y l)))))))

)
