
(cl-text s_arithmetic

(forall (x)
	(not (= (S x) zero)))

(forall (x y)
	(if	(= (S x) (S y))
		(= x y)))

(forall (y)
	(if	(not (= y zero))
		(exists (x)
			(= y (S x)))))

(forall (x)
	(not (= x (S ...))))

)
