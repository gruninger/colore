
(cl-text directed_periods

(cl-imports periods)

(cl-comment "DIR")
(forall (x y)
                (exists (z)
                                (and    (inclusion x z)
                                        (inclusion y z))))

)
