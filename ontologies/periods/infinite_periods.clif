
(cl-text infinite_periods

(cl-imports periods)

(cl-comment "SUCC-1")
(forall (x)
                (exists (y)
                                (precedence x y)))

(cl-comment "SUCC-2")
(forall (x)
                (exists (y)
                                (precedence y x)))


)
