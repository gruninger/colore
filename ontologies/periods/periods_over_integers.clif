(cl-text periods_over_integers

(cl-imports mixed_periods)

(cl-comment "ATOM")
(forall	(x)
		(exists (y)
				(and	(inclusion y x)
						(forall	(z)
								(if	(inclusion z y)
									(= x y))))))
)	
