
(cl-text linear_periods

(cl-imports periods)

(cl-imports periods_def)

(cl-comment "LIN*")
(forall (x y)
(or             (precedence x y)
                (precedence y x)
                (overlaps x y)))

)
