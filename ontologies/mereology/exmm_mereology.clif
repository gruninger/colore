
(cl-text exmm_mereology

(cl-imports mm_mereology)

(cl-comment "Extensionality")
(forall (x y w)
        (if     (and	(ppart w x)
			(not (= x y)))
                (exists (z)
                        (iff    (ppart z x)
                                (not (ppart z y))))))

)
