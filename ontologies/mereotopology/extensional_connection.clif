
(cl-text http://colore.oor.net/mereotopology/extensional_connection.clif

(cl-imports http://colore.oor.net/mereotopology/connection.clif)

(cl-comment "C-Ext")

(forall (x y)
    (if
        (forall (z)
            (iff    (C x z)
                    (C y z)))
        (= x y)))

)
