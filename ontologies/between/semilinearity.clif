(cl-text http://colore.oor.net/between/semilinearity.clif

(cl-imports http://colore.oor.net/between/weak-bet.clif)

(forall (x y z w)
        (if     (between y x z)
                (or     (between y x w)
                        (between z x w))))

)

